android {
            defaultConfig {
                applicationId = "cdiamante.controldiamante.com"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }